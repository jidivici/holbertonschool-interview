# Star Wars API

Scripts that interact with the Star Wars API (swapi-api.hbtn.io).

## Scripts

- `0-starwars_characters.js` - Prints all characters of a Star Wars movie, given its Movie ID as the first positional argument.

## Requirements

- Node 10.14.x
- `request` module
- semistandard compliant code
